(()=>{const tg=window.Telegram?.WebApp,root=document.documentElement,status=document.querySelector('#status'),name=document.querySelector('#name'),avatar=document.querySelector('#avatar');if(tg){tg.ready();tg.expand();status.textContent='Telegram';const u=tg.initDataUnsafe?.user;if(u){name.textContent=u.username?'@'+u.username:[u.first_name,u.last_name].filter(Boolean).join(' ')||'Telegram';avatar.textContent=(u.first_name||'?')[0].toUpperCase()}}else status.textContent='Браузер';document.querySelectorAll('[data-send]').forEach(b=>b.onclick=()=>{if(tg){tg.sendData(JSON.stringify({type:'action',action:b.dataset.send}));tg.close()}else alert('Откройте приложение через Telegram.')});addEventListener('mousemove',e=>{root.style.setProperty('--mx',e.clientX/innerWidth*100+'%');root.style.setProperty('--my',e.clientY/innerHeight*100+'%')},{passive:true})})();
